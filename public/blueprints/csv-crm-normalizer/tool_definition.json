{
  "name": "csv_crm_normalizer",
  "description": "Standardizes messy CRM data (names, emails, phones) into a clean format.",
  "runtime": "python",
  "entrypoint": "clean_data.py",
  "install": "pip install -r requirements.txt",
  "test": "python tests.py",
  "configuration": {
    "file": "config.json",
    "description": "Modify this JSON to map columns or change formatting rules."
  },
  "inputs": {
    "description": "A CSV file with raw contact data.",
    "default_filename": "messy_crm_export.csv"
  },
  "outputs": {
    "description": "A standardized CSV ready for import.",
    "default_filename": "final_import_ready.csv"
  }
}