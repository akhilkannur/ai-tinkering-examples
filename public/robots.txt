# Real AI Examples - Robots.txt
# Strategy: Allow crawling everywhere so Google can discover noindex meta tags
# on deprecated pages and de-index them. Disallow blocks crawling, which
# PREVENTS Google from seeing noindex, keeping pages indexed forever.

User-agent: *
Allow: /

# Only block truly private/API paths
Disallow: /api/
Allow: /api/og
Disallow: /debug

# Crawl delay
Crawl-delay: 1

# Sitemap location
Sitemap: https://realaiexamples.com/sitemap.xml
